{
  "name": "@friday/friday",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "auth-service/service"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "dev": "lerna run dev --parallel",
    "clean": "lerna clean"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "lerna": "8.2.2"
  }
}
